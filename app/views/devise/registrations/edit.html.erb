<div class="container-fluid">
<div class="row">
  <div class="col-lg-offset-3 col-lg-6" >
    <div id="form-container" class="container-fluid">
      <h2 class="col-sm-offset-4">Editar datos personales</h2>
      <%= form_for(resource, :as => resource_name, :url => registration_path(resource_name), :html => { :method => :put, :class => "form-horizontal", :role => "form" }) do |f| %>
      <%= devise_error_messages! %>
      <div class="form-group">
        <%= f.label :name, "Nombre", :class => "col-sm-4 control-label" %>
        <%= f.text_field :name, :autofocus => true, :class => "col-sm-7" %>
      </div>
      <div class="form-group">
        <%= f.label :email, "Correo", :class => "col-sm-4 control-label" %>
        <%= f.email_field :email, :class => "col-sm-7" %>
      </div>
      <div class="form-group">
        <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
          <div>Esperando confirmación para: <%= resource.unconfirmed_email %></div>
        <% end %>
      </div>
      <div class="form-group">
        <%= f.label :password, "Contraseña", :class => "col-sm-4 control-label"%> <i>(déjalo en blanco si no quieres cambiarla)</i>
        <%= f.password_field :password, :autocomplete => "off", :class => "col-sm-7" %>
      </div>
      <div class="form-group">
        <%= f.label :password_confirmation, "Confirmar contraseña", :class => "col-sm-4 control-label" %>
        <%= f.password_field :password_confirmation, :class => "col-sm-7" %>
      </div>
      <div class="form-group">
        <%= f.label :current_password, "Contraseña actual", :class => "col-sm-4 control-label" %> <i>(necesitamos tu contraseña actual para confirmar los cambios)</i>
        <%= f.password_field :current_password, :class => "col-sm-7" %>
      </div>
      <div class="form-group">
        <%= f.submit "Actualizar datos", :class => "btn btn-primary col-sm-offset-5" %>
        <% end %>
      </div>
      <div class="form-group">
        <h3>Eliminar la cuenta</h3>
        <p>¿No te gustó la volá? <%= button_to "Pitearse la cuenta", registration_path(resource_name), :data => { :confirm => "¿De verdad te la querís pitear?" }, :method => :delete, :class => "btn btn-danger col-sm-offset-5" %></p><br/>
        <%= link_to "Volver", :back %>
      </div>
    </div>
  </div>
</div>


